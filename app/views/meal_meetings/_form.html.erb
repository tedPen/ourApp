<%= form_for(@meal_meeting) do |f| %>
  <% if @meal_meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meal_meeting.errors.count, "error") %> prohibited this meal_meeting from being saved:</h2>

      <ul>
      <% @meal_meeting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :interests %><br />
    <%= f.text_area :interests, :rows => 6 %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.text_field :time %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>